import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

import java.util.List;

public class ArrayBoxTest {
    private ArrayBox arrayBox;
    @Before
    public void init()
{
    arrayBox = new ArrayBox();
}
   // @Test
    /*public void verify()
{
    Integer [] elements = new Integer[]{4,5,6};
    elements = new ArrayList<Integer>(Array.asList(elements));
    arrayBox.setList(list);
    int sum = arrayBox.countOddNumber();
    Assert.assertEquals(15, sum);
}*/
}
